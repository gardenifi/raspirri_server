# actions/common-steps-action/action.yml

name: Common Steps Action
description: Run common steps for workflows
runs:
  using: 'composite'
  shell: bash
  steps:
    - run: echo "Running common step 1"
    - run: echo "Running common step 2"
    # Add more common steps as needed
