name: Tests

on:
  push:
    branches:
      - main
    paths:
      - 'tests/**'
      - 'raspirri/**'
      - '.github/workflows/**'
      - '.pre-commit-config.yaml'

jobs:
  pre-commit:
    uses: gardenifi/raspirri_server/.github/workflows/precommit.yml@main
    with:
      fake-argument: 'fake'
