name: Tests

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'bumpversion.cfg'

jobs:
  pre-commit:
    uses: gardenifi/raspirri_server/.github/workflows/precommit.yml@main
    with:
      fake-argument: 'fake'
