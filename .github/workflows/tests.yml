name: Tests

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'CHANGELOG.md'

jobs:
  common-steps:
    uses: gardenifi/raspirri_server/.github/workflows/common-steps.yml@main
    with:
      fake-argument: 'fake'
